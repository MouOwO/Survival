# 存档 UI Kit V1 接入

已阅读包内实际文件 AI实现说明.md 与 manifest.json，使用 progress_mockup.png 的1672×941坐标：主体1002×758居中、左栏240、卡片162×154、四列三行、步长174×164。外层只进行一次等比缩放并扣除Panorama已有UI缩放。55%黑色蒙层与弹窗是兄弟控件，点击外部关闭，内部交互不冒泡到蒙层；关闭按钮和取消事件保留。接入现有SurvivalUILayers。

新样式 archive_kit.css 替代 archive_gothic.css。共用导航组件由服务端分类生成，九个参考分类优先排列，其他现有分类追加在滚动导航里，不删除既有功能。图标四状态来自包内icons_rgba。入口、标题、关闭均使用新资源，没有用字符代替图标。

卡片图案：已有积分/建筑物品图保留；原先没有独立资源的符文占位改为包内按icon_style映射的装饰性存档徽记（不是新物品或新奖励）。进度使用真实count/target，缺字段隐藏，不默认required=10/1，不裁成截图数值。解锁标签仅在现有业务提供completed/unlocked状态且语义适用时显示；没有可靠解锁字段的收藏不猜测状态。筛选只在客户端展示已知状态，不发奖励请求。图标、名称、进度与状态均分层。

注意：包内background_tiles/card_slice_11.png实际含烘焙的示例名称和解锁标签，边片也可能带文字边缘；本轮全部弃用这组原始卡片切片。clean_slices由无字reusable/card_normal.png导出，运行时12设计单位角块固定、边条单向伸缩、中心填充。

保留了抽取/升级/晋升/建筑信仰值/钓鱼库存/在线进度及真实tooltip属性。抽取页压缩标题区以预留固定操作栏，仍保留完整三行，不让列表从操作栏背后露出。

验证：test_archive_ui.js、test_archive_gothic_ui.js、test_archive_kit_assets.js通过；76条PNG引用存在且编译成功。verify.cjs检查12张完整可见、无缺图。preview.png、side.png、overlay.png均为浏览器布局验证，使用真实配置名称/上限和零进度测试数值，不代表玩家存档或实际游戏截图；实际游戏截图无法在当前工具中获取。compile.log记录本地编译部署。

字体：压缩包不含字库，本机未找到Source Han/Noto字体，系统有宋体和微软雅黑；样式明确配置这些回退，尚未在游戏内确认实际命中的字体。不会声称思源字体已生效。底板是包内重建主体，不包含突出冠饰，没有把参考地图背景覆盖到实时游戏中。
