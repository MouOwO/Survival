const fs=require('fs'),path=require('path'),cp=require('child_process'),crypto=require('crypto');
const root=path.resolve(__dirname,'..'),out=path.join(root,'output/basin_natural'),shots=path.resolve(root,'../../dota/screenshots');
const vpk=path.join(root,'maps/survival_basin_natural.vpk'),built=fs.statSync(vpk).mtimeMs;
const list=[['overview','单平台整体'],['ground','冷色石地与开放场地'],['shore','浅水与湿润岸边'],['entry','保留的原生入口']];
const captures=list.map(([key,label])=>{const files=fs.readdirSync(shots).filter(f=>new RegExp('^basin_natural_'+key+'_\\d+\\.tga$').test(f)).map(f=>({name:f,time:fs.statSync(path.join(shots,f)).mtimeMs})).sort((a,b)=>b.time-a.time);if(!files.length||files[0].time<built)throw Error('Missing fresh capture '+key);cp.execFileSync(process.execPath,[path.join(__dirname,'capture_dota_image.cjs'),files[0].name,path.join(out,key+'.png')]);return{key,label,...files[0]};});
fs.writeFileSync(path.join(out,'capture_manifest.json'),JSON.stringify({builtAt:new Date(built).toISOString(),vpkSha256:crypto.createHash('sha256').update(fs.readFileSync(vpk)).digest('hex'),captures},null,2));
const log=fs.readFileSync(path.resolve(root,'../../dota/console.log'),'utf8');
const checks=log.split(/\r?\n/).filter(l=>l.includes('[NATURAL_PATH]')).slice(-4);
fs.writeFileSync(path.join(out,'runtime_checks.json'),JSON.stringify({checkedAt:new Date().toISOString(),map:'survival_basin_natural',groundChecks:checks,allSamplePathsPass:checks.length===4&&checks.every(l=>/true\s+true\s*$/.test(l))},null,2));
fs.writeFileSync(path.join(out,'index.html'),`<!doctype html><html lang="zh-CN"><meta charset="utf-8"><title>自然石岸 · 实机样板</title><style>body{margin:0;background:#172c36;color:#e1edf0;font:17px/1.7 system-ui}main{max-width:1500px;margin:auto;padding:28px}h1{font-size:28px}figure{margin:28px 0}img{width:100%;display:block}figcaption{padding:10px}code{background:#2b4652;padding:5px}a{color:#afe4e7}</style><main><h1>自然石岸 · 首版实机样板</h1><p>冷青灰石地、局部浅水与缓坡岸边，保留中央活动空间及西侧入口。本页为实际游戏截图。</p><p><a href="approved_concept.png">打开已确认的概念参考图</a>。当前为首版材质与水岸样板，石地风化、岸边碎石层次及远景仍比概念图简化。</p><p>样板地图：<code>survival_basin_natural.vmap</code>。你手工修改的 <code>survival_basin_native.vmap</code> 保留。</p>${captures.map(s=>`<figure><a href="${s.key}.png"><img src="${s.key}.png" alt="${s.label}"></a><figcaption>${s.label}</figcaption></figure>`).join('')}</main></html>`);
console.log(path.join(out,'index.html'));
