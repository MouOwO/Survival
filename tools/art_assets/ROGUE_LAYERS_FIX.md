# 肉鸽分层修正（2026-09-13）

- 保留 cleanBase 米白底图，以 Panel 背景按 290×672 完整缩放，避免 Image 原始尺寸裁切。
- 恢复已确认 base 的顶部、左右边框和完整底部文字框裁片；裁片不包含原有剑插画。
- 插画为独立第二层，范围 (29,50,232,348)，不进入分隔线和文字区域。
- 文案为第三层，范围 (27,454,236,168)，长文本沿用滚动显示。
- 作者脚本、正式脚本、候选脚本与作者/正式/候选样式均同步；content 脚本和样式强制编译成功。
- tools/test_rogue_reward_ui.js、test_rogue.cjs、test_art.cjs 通过。素材测试仅验证现有 52 张图的映射、哈希及 RGBA 格式，不代表背景抠图质量通过。

未完成：插画背景精确移除。内置 imagegen 两次输出 RGB 棋盘格/背景，没有真实 alpha，均未采用，保留原有 RGBA 素材。生成结果位于 Codex generated_images，未接入项目，也未替换已确认原图。提示要求保持角色、斧头、树桩、颜色及构图，仅去掉环境背景并输出真透明 PNG。
未验证：游戏内实际视觉。尝试激活 Dota 2 窗口失败，不能把静态及编译检查算作游戏验收。
