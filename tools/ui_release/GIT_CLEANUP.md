# Git 跟踪文件清理

扫描了 1,978 个 Git 跟踪路径，删除 40 个仍在工作区的生成文件，共 35,732,265 字节（约 34.1 MiB）。逐文件原因、大小和 SHA256 见 `git_cleanup.json`。

清理对象为历史浏览器预览/对比截图、编译日志与 Python 字节码缓存。实际运行资源、图片源文件、配置、有效测试、构建脚本及交接包继续保留。尤其保留 `output/lottery_reference_review/reference-1045.png` 和 `output/lottery_v2_preview/pool_details_reference.png`，它们仍参与历史裁切/对比流程，不能仅按“效果图”删除。

备份：`D:/survival_ui_backups/release_5d5c1152eb/git_tracked_cleanup.zip`，删除前逐文件校验 SHA256。历史文档中指向已删除截图的链接可按原相对路径在该备份内查找；预览工具也可重新生成截图。

`.gitignore` 增加明确的生成截图路径、录屏、日志和浏览器缓存规则，没有忽略全部 PNG 或整个 output，以保留必要的美术源与构建输入。

清理后检查：Git 正确报告这 40 项工作区删除；正式发布清单中的 81 个代码/布局源、2,291 个图片源、1,909 个运行文件仍全部存在。本轮只验证文件完整性，没有重新进行游戏内视觉验收。

删除尚未提交或推送。本轮没有改写 Git 历史；删除提交进入远端后，最新版文件树会减少，但旧提交中的图片对象仍占用历史仓库空间。
