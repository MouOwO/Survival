# UI 工作目录整理

原 `spikes`、`output` 已删除。有效内容迁到同深度的 `ui`（构建、测试、基线及交接源码）与 `design`（美术源及导入工具），保留脚本相对目录结构。正式 `panorama` 游戏资源未改动。

迁移 6,789 个文件；清除 4,400 个历史证据、截图、缓存、日志、录像与备份文件，共 1,756,352,639 字节，约 1.64 GiB。不论是否被 Git 跟踪均纳入整理。

全量备份：`D:/survival_ui_backups/release_5d5c1152eb/workspace_reorganization.zip`。包含原目录全部 11,189 个文件和修改前的外部工具，原文件及迁移文件均逐项校验 SHA256。逐文件去向、清理原因与修改工具列表见 `reorganization.json`。

同步修改 33 个外部工具/文档及 `.gitignore` 中的路径，另修正正式发布规划脚本 `plan.py`。未改写已有发布/清理 JSON 收据中的历史路径与哈希，以免篡改历史记录。旧文档的截图链接可能失效，请按原路径在备份查找。

验证：每日奖励、抽奖/结果生命周期、肉鸽、商城模拟六状态测试通过，主要构建脚本语法通过；正式清单 81 个源码、2,291 个图片源、1,909 个运行文件仍存在。此次未运行游戏做新的视觉验收。

已知旧测试：`ui/remaining_ui_handoff_v1/test_commerce.cjs` 第 10 行要求购买按钮数为 0，实际为 8；迁移前原目录测试同样失败，当前模拟商城使用独立的 `ui/shop_preview_v1/test.cjs` 且通过。未为此次整理修改业务实现或放宽该旧断言。

candidate、baseline、切图源和交接包仍有构建/测试/部署引用，予以保留。本轮没有提交或推送 Git，也没有改写历史。
