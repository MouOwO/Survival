const fs = require('fs'), path = require('path');
const root = path.resolve(__dirname, '../output/gold_training_room');
const dest = path.join(root, 'material_review');
const comparisons = [['stone_coin', '石材与金币浮雕'], ['bronze_stone', '青铜火盆与地坪']];
const cards = comparisons.map(([name, title], i) => `<section><h2>${title}</h2><div class="compare" style="--cut:50%"><img src="${name}_after.png" alt="修改后"><div class="before"><img src="${name}_before.png" alt="修改前"></div><div class="divider"></div><span class="left">修改前</span><span class="right">修改后</span></div><label for="slider${i}">拖动比较</label><input id="slider${i}" type="range" min="0" max="100" value="50" aria-label="${title}前后对比" oninput="this.parentElement.querySelector('.compare').style.setProperty('--cut',this.value+'%')"><p><a href="${name}_before.png">查看原图</a> · <a href="${name}_after.png">查看修改后</a></p></section>`).join('');
const runtimeReport = path.join(dest, 'runtime_verification.json');
const visuallyChecked = fs.existsSync(runtimeReport) && JSON.parse(fs.readFileSync(runtimeReport, 'utf8')).visual_review_complete;
const runtime = visuallyChecked ? ['runtime_material_overview', 'runtime_material_bronze', 'runtime_material_coin'].filter(n => fs.existsSync(path.join(dest, n + '.png'))) : [];
const runtimeHtml = runtime.length ? `<section><h2>本轮游戏内截图</h2>${runtime.map(n => `<a href="${n}.png"><img class="runtime" src="${n}.png" alt="金币练功房实际游戏画面"></a>`).join('')}</section>` : '';
fs.writeFileSync(path.join(dest, 'index.html'), `<!doctype html><html lang="zh-CN"><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>金币练功房 · 材质对比</title><style>*{box-sizing:border-box}body{margin:0;background:#202724;color:#eee9dc;font:16px/1.7 system-ui,"Microsoft YaHei",sans-serif}main{max-width:1200px;margin:auto;padding:32px 24px}h1{margin:6px 0}h2{font-size:22px;margin-top:35px}a{color:#e6c985}p{color:#c9cec4}section{margin-bottom:28px}.compare{position:relative;overflow:hidden;aspect-ratio:4/3;background:#111}.compare img{display:block;width:100%;height:100%;object-fit:cover}.before{position:absolute;inset:0;clip-path:inset(0 calc(100% - var(--cut)) 0 0)}.divider{position:absolute;left:var(--cut);top:0;bottom:0;border-left:2px solid #fff}.left,.right{position:absolute;top:12px;padding:4px 10px;background:#111a}.left{left:12px}.right{right:12px}input{display:block;width:100%;accent-color:#dcba72}label{display:block;margin-top:8px}.runtime{width:100%;display:block;margin:14px 0;border:1px solid #62675c}.note{padding:14px 18px;background:#303b34;border-left:3px solid #d9b874}</style><main><a href="../index.html">← 金币练功房总览</a><h1>金币练功房 · 材质对比</h1><p>石面起伏与磨损 · 铜器氧化与磨亮区域 · 金币高光</p><p class="note">下方滑动对比为 Blender 同角度、同光照渲染，模型和摆放一致。游戏效果以实机画面为准。此次材质调整仅用于金币练功房。</p>${runtimeHtml}${cards}<h2>测试地图</h2><p>Workshop 控制台运行 <code>dota_launch_custom_game survival gold_training_room_review</code>；进入后可运行 <code>ent_fire 0 RunScriptCode &quot;require('tests/manual_gold_room_review').start()&quot;</code> 查看全景。</p><p><a href="../gold_training_room.blend">Blender 场景</a> · <a href="../material_manifest.json">材质检查清单</a></p></main></html>`);
console.log('GOLD_MATERIAL_REVIEW', comparisons.length, 'comparisons;', runtime.length, 'runtime captures');
