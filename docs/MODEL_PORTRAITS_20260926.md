# 罪渊模型与原生动态头像修复（2026-09-26）

用户确认罪渊表现为缺部件、错乱或 ERROR；左下角头像保留动态 3D，调整镜头和背景。

## 罪渊

- 十层具名 NPC 先前全部使用 Undying 基础模型，再在出生后 SetModel。现单位 KV 的初始 Model 与 monster_archetypes 对齐；名称同步生成器也按声明生成模型。
- 第 3 层炼金术士缺少默认的地精头和食人魔头；第 6 层帕吉仅有至宝背部，缺武器、副手、肩部、护腕、头发、腰带；第 9 层痛苦女王缺至宝适配腿部。按安装版本 Valve items_game.txt 补齐 9 个部件，十层合计 52 个挂载件。
- 帕吉 item 7756 默认红色款的主体 skin 为 1，普通饰品为 0；补齐 arcana、arcana_back 动作标记。痛苦女王补齐 arcana；腿部使用 item 12930 对默认 item 781 的官方模型替换路径。
- CSV 仍为运行数据权威，只更新 asset_components、asset_catalog、asset_activity_modifiers 的相关记录，并通过 build_configs.build 重新生成对应 Lua。同步维护 build_boss_cosmetic_batch.py 及三个资源预加载代理。
- 挑战预加载原先通过主体模型反查资源包；同一主体可能对应多个套装。现在优先使用出生路径声明的 model_asset_id / default_wearable_asset_id。
- 出生外观处理失败会输出 TEN_SINS_VISUAL_FAILED（成员、模型、原因），不再完全吞掉异常。此次确认了缺件和配置不一致，未将所有 ERROR 现象归因于同一处。

## 动态头像

新增 scripts/npc/portraits_custom.txt，通过客户端已支持的模型路径配置复用原生动态头像。没有新增每个单位的 ScenePanel 或复制单位。

覆盖 34 个自定义建筑/城墙、8 个原生塔、8 级伐木工及 2 级修理工，共 52 个模型。建筑及缺少镜头的工人根据编译模型真实渲染包围盒计算取景；已有 Valve 镜头的 5 个模型保留其构图并扩大 12% 视野留白。背景统一为暗青渐变，暖主光配柔和环境光。原生多选头像与切换恢复逻辑保持。

生成工具：tools/build_unit_portraits.py。只写逐模型条目，不覆盖全局 default。模型更新后应重新运行；头像 KV 为直接读取的文本资源，无需编译 Panorama。

## 验证与限制

- 5 项 Lua 回归通过：罪渊外观挂载/清理、启动预加载、挑战名称、玩家挑战清理、建筑死亡外观；多选头像 Node 回归通过。
- 新外观回归使用真实资源目录、外观挂载服务和动作服务，模拟引擎实体；旧配置能复现第 3 层缺件断言失败。
- 校验十层 62 个主体/部件资源均存在于本机 Valve VPK，新增部件进入对应 precache 代理。CSV 逐记录对比仅改变目标三层的相关外观数据。
- 47 个新计算镜头通过包围盒八角点投影视锥检查；另 5 个沿用 Valve 构图。NPC 与头像 KV 解析、Lua 5.1 与 JS 语法检查通过。
- 本轮 Dota 2 未运行，没有完成实机渲染验收。镜头、灯光的最终视觉效果，以及用户报告的全部 ERROR 是否消失，仍需完整退出游戏后重开 Workshop 对局确认。

备份、官方参考、模型包围盒及验证记录：output/model_portraits_20260926/。
