# 2026-09-26 偶发粒子渲染闪退

三份16:32:21、16:37:28、16:41:42转储均为 `particles.dll+0x42428`、`0xc0000005`，向 `0x8f0` 写入，RAX=0。现场指令 `mov qword ptr [rax + 0x8f0], rcx` 及前后指令表明故障发生在粒子链表摘除路径。工具提供的是栈内代码指针候选，不是完整符号化调用栈；具体粒子ID、资源名称及项目侧触发源仍未知。用户确认其他操作也偶发。

转储发生在上一轮17:23图标改动前。本地化缺项是独立问题，不能当作native崩溃根因。上一轮已为Doom等六英雄补齐玩家账号饰品异步预加载门禁，本轮回归通过，尚无实机证据证明其消除了本次崩溃。

修正四处清理所有权：英雄饰品、怪物视觉、怪物饰品、攻击范围圈均先清除活动记录再调用引擎，避免清理重入重复销毁/释放，以及回调新状态被旧清理覆盖。新增Lua回归在旧代码上复现重复销毁，新代码四路径及新状态保留通过；另三项英雄预载/召唤回归通过。普通/高级研究所双语名称及别名已补齐；后续图标任务检查时发现中文经PowerShell管道编码丢失，已修正为正确UTF-8文本。

证据：`output/crash_20260926/dump_analysis.json`；修改前备份在同目录 `before/`；解析工具 `tools/inspect_minidump.py`。

**状态：代码缺陷已修正，native闪退根因及消失情况待实机验证。** 模拟测试无法证明Source 2不会再崩溃。

复测需完全退出Dota和Workshop Tools，再开新局，召唤英雄后反复切换单位、普通/高级研究所，测试范围拾取、攻击范围、建造升级及战斗。若复发，对照新转储与完整日志单独隔离特效来源，不同时禁用所有特效后宣称根因已确定。
