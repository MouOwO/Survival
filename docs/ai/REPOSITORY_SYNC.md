# 2026-09-19 双仓库清理与配套交付

game `dev` 保存运行资源、脚本、生成器及测试，content `master` 保存可编辑地图和材质、粒子源码。主地图仍为 `template_map`，最近认可的中央区域及近景优化保持不变；C6 作为完整源图保留。两仓需取用配套提交，content 地图通过 Git LFS 获取。

旧地图输出、诊断快照、日志、缓存、编辑器历史和指向本机的美术快捷方式已清理。原图与必要生成输入保留，当前参考图与验收摘要迁入 `art/maps/c6/` 和 `docs/ai/validation/20260919/`。临时文件不再作为重建前提；新机器的预览与编译步骤见 `tools/map_c6/README.md`。

两个已有分支合并及最新 stash 恢复已完成；旧 stash 与 Git 备份引用保留。恢复内容包含中央地图、兼容资源和运行修正。前一步 23 次 UI 编译、68 个语法检查及 16 项回归通过，本轮另外检查清理后的重新生成与文件完整性。最终记录见 `docs/ai/validation/20260919/repository-cleanup.json`。

# 2026-09-17 双仓库同步与资源交付（历史）

## 合并

- game `dev` 合并远端 `ffbc67e0`；保留原本的本地提交与建筑、地图改动。
- content `master` 更新至 `236d6c3` 后恢复本地资源。
- 首次合并的四个 UI 二进制冲突：反编译确认本地内容与共同祖先相同，采用远端新版。
- stash 恢复的六个 UI 二进制冲突从最终 content 源码重新编译。两个 XML 冲突保留已包含本地新增引用的新版布局。
- 八个未跟踪 UI 文件与远端新增文件重名：相同文件直接复用，其余保留远端已发布的存档与每日奖励界面。
- 同步 content 中较新的 UI 修正至 game 作者源码；保留 `ui-resource://` 的资源转换约定。

## 文件边界

- content：Hammer 地图、prefab、模型与材质源文件、粒子源文件。
- game：Lua/CSV、编译后的运行资源、Blender 工程、生成器、所需构建输入、资源清单与预览。
- 原生资源研究中仍被生成器读取的素材与模板继续保留。
- 清理崩溃转储、Python 缓存、Blender 自动备份、合并工具残留、过期地形检查点、未使用的完整外部参考地图转储与编译日志。
- 当前三个大地图文件使用 Git LFS；首次取用请执行 `git lfs install` 和 `git lfs pull`。

## 验证

- 23 项 Panorama 源文件编译成功。
- 18 项建筑、占格、升级、城墙摧毁与碰撞回归通过；7 项 UI 回归通过。
- 24 款建筑、10 款城墙、流光外壳、摧毁粒子、金币房、主岛、熔火房共 7 项资源验证通过。
- 12 个地图 VPK 的目录与逐项 CRC32 校验通过；损坏包反例被拒绝。
- 资源检查不再依赖 Git 不保留的文件修改时间或本机旧日志，改查安装源文件一致性、实际编译资源内容与 VPK 校验码。
- CSV 清理步骤保留由资源生成器维护的白光外壳和城墙摧毁映射。

以上为本轮合并后的编译与离线验证；各场景此前的实机结果见对应资源文档，本轮未重新进行游戏内全流程验收。
