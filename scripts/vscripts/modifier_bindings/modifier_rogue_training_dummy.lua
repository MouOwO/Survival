-- Generated by tools/build_modifier_bindings.ps1. Engine script entry; do not require this file.
require("modifiers/modifier_rogue_combat_effects")
-- require may be cached and populate _G rather than this engine script scope.
local definition = assert(rawget(getfenv(1), "modifier_rogue_training_dummy") or _G.modifier_rogue_training_dummy, "Missing modifier class: modifier_rogue_training_dummy")
assert(type(definition) == "table", "Invalid modifier class: modifier_rogue_training_dummy")
modifier_rogue_training_dummy = definition
return definition
