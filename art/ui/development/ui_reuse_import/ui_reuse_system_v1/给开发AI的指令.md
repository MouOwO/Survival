请在我们的Dota 2“苟发育”项目中建立统一UI组件与资源复用体系。先阅读本包的开始阅读.md及docs，然后检查项目现有UI实现。保留现有框架和业务逻辑，执行整理与迁移，不继续给每个页面生成一整套新的图片。

1. 使用resource_registry.json的稳定资源ID和legacy_aliases.json的旧→新映射。所有页面通过同一解析层引用图片，不直接散落硬编码旧路径。
2. 建立ModalShell、FullscreenShell、HUDShell三类外层。存档、商城、奖池详情与抽奖记录共用同一个弹窗壳，只传标题、尺寸、页签、侧栏和内容。主HUD保留轻量结构。
3. 弹窗外显示实时游戏＋统一半透明遮罩，不能把效果图地图当运行背景。全屏美术页将背景、主题画、渐暗层和控件分开；同主题直接复用同一个背景ID。
4. 按component_contracts.json建立共用IconButton、ActionButton、NavToggle、TabBar、CardShell、Checkbox、ProgressBadge、Tooltip等。页面通过props传入数据，不复制组件代码。ProductCard、BundleCard、VipBundleCard是内容变体，商城五分栏共用同一外层。
5. 主界面导航图标统一使用当前极简灰白版本；保留正确的功能语义。关闭当前窗口与官方退出必须保持不同事件，禁止因为共用图标而合并行为。
6. normal/hover/pressed/selected/disabled/busy统一管理，所有态保持同一显示尺寸。顶部图标仅提亮；肉鸽牌悬停放大103.5%，不能把此效果套给所有按钮。
7. 旧exact裁片继续作视觉基准。新包中的共用资源来自旧包，但估算alpha、程序状态和补绘底板仍需一次集中视觉校准。别将带背景裁片当透明图片；别把生成候选说成原图无损切片。
8. 共用窗口边框目前缺少干净透明九宫格。选定一个母版后只精修一次，记录四角、四边、slice内距和最小尺寸，全窗口共用；先完成结构和引用，不为每页重新切框。详见补齐清单。
9. 价格、币种关系、VIP资格、倒计时、存档进度、奖励、概率全部接现有数据。抽奖先锁定真实结果再播放动画；肉鸽用原候选，不从美术示例数值重建规则。
10. 先检查Panorama根面板逻辑分辨率及自动缩放。旧1672×941坐标只做一次转换，不让每张PNG单独做屏幕比例缩放。透明画布边距、图标本体和命中框分别处理。
11. 按“资源注册→共用状态页→弹窗壳→存档与商城→详情与记录→全屏抽奖与肉鸽→HUD”的顺序迁移。引用验证成功后再停用旧路径；未确认项目引用前不删除旧资源。
12. 运行本包的verify_registry.py核对资源和映射。实际游戏内另验收布局、黑边、点击穿透、禁用态、分辨率和多次开关。交付改动文件、公共组件引用表、状态样例截图与游戏实机截图。

后续增加页面时：先组合现有组件；只有新语义图标、新主题立绘或无法表达的新单元才增加素材，并写进注册表。目标是维护同一套青蓝、玉白、细香槟金的极简游戏UI，避免每个页面另起一套。
